services:
  - type: web
    name: ai-gpu-brain-v2
    env: python
    pythonVersion: "3.11"  <-- ADD THIS LINE
    region: oregon
    plan: free
    buildCommand: "cd ai-engine && pip install -r requirements.txt"
    startCommand: "cd ai-engine && uvicorn main:app --host 0.0.0.0 --port 10000"
